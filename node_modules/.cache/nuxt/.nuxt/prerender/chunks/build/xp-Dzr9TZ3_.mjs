function calculateWorkoutXp(params) {
  const base = 50;
  const streakBonus = Math.min(params.streakDays, 7);
  const xp = base + streakBonus;
  const coins = 10 + 0;
  return { xp, coins };
}
function formatDuration(seconds) {
  const h = Math.floor(seconds / 3600);
  const m = Math.floor(seconds % 3600 / 60);
  if (h > 0) return `${h}h ${m}m`;
  return `${m}m`;
}
function calcTotalVolumeKg(exercises) {
  return exercises.reduce((total, ex) => {
    if (ex.exerciseType !== "standard" && ex.exerciseType !== "assistedBody") return total;
    return total + ex.sets.filter((s) => s.completed).reduce((sum, s) => sum + (parseFloat(s.weight) || 0) * (parseInt(s.reps) || 0), 0);
  }, 0);
}

export { calcTotalVolumeKg as a, calculateWorkoutXp as c, formatDuration as f };
//# sourceMappingURL=xp-Dzr9TZ3_.mjs.map
