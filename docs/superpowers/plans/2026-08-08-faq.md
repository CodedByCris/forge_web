# FAQ — CMS + Backend + App móvil — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Colección `faq` nueva en Firestore (pública en lectura, solo-admin en escritura), módulo de gestión en el CMS (`/cms/faq`), y reemplazo del contenido fijo de `FaqScreen` en la app móvil por lectura real de esa colección.

**Architecture:** CMS — mismo patrón `services → stores → components/pages` ya usado en Usuarios, 100% client-side. App móvil — Clean Architecture + Riverpod, replicando exactamente el patrón ya usado en `features/goals` (datasource → repository → provider), pero solo lectura (sin `StateNotifier`, ya que la app no escribe FAQ).

**Tech Stack:** Nuxt 4/Pinia/Firebase JS SDK (CMS). Flutter/Riverpod/`cloud_firestore` (app, todas dependencias ya existentes). Cero dependencias nuevas en ningún repo.

**Spec:** `docs/superpowers/specs/2026-08-08-faq-design.md`

---

## Nota sobre este plan

- **Commits en `forge_web` (CMS): autorizados explícitamente por el
  usuario.** Se hace un commit al final de la sección CMS (Task 8), tras
  verificar que todo funciona — no un commit por archivo.
- **Commits en `forge` (app móvil y `firestore.rules`): NO autorizados.**
  Todo ese trabajo queda en el working tree sin commitear.
- **Deploy de `firestore.rules`**: el usuario ya autorizó explícitamente
  ejecutar deploys de Firestore rules sin pausar a pedir confirmación en
  este proyecto (ver Task 1). Se hace `--dry-run` primero de todas formas,
  por disciplina, no por pedir permiso.
- Sin tests en ningún repo (regla ya establecida).

---

### Task 1: Regla nueva `faq` en `forge/firestore.rules` + deploy

**Files:**
- Modify: `/Users/cris/Desktop/forge/firestore.rules`

- [ ] **Step 1: Añadir el bloque nuevo**

Añadir, junto a los demás bloques `match` de nivel superior (por ejemplo
después del bloque `users`), un bloque completamente nuevo — no modifica
ningún bloque existente:

```javascript
    match /faq/{faqId} {
      allow read: if true;
      allow write: if isAdmin();
    }
```

- [ ] **Step 2: Verificar con dry-run**

Run (desde `/Users/cris/Desktop/forge`):
```bash
firebase deploy --only firestore:rules --dry-run
```
Expected: `rules file firestore.rules compiled successfully` / `Dry run complete!`.

- [ ] **Step 3: Desplegar**

Run:
```bash
firebase deploy --only firestore:rules
```
Expected: `Deploy complete!`.

- [ ] **Step 4: Verificar manualmente el resultado real**

Lectura anónima (sin token) a la colección `faq` — debe funcionar aunque no
haya ningún documento todavía (devuelve lista vacía, no `403`):
```bash
curl -s "https://firestore.googleapis.com/v1/projects/gym-app-41fd6/databases/(default)/documents/faq"
```
Expected: JSON válido sin `"code": 403"` (puede no tener campo `documents`
si la colección está vacía — eso es normal, lo que importa es que no sea un
error de permisos).

No se hace `git commit` de `firestore.rules` (regla de este plan).

---

### Task 2: Tipos CMS (`CmsFaq`)

**Files:**
- Create: `app/types/cms/faq.ts`

- [ ] **Step 1: Escribir los tipos**

```typescript
// app/types/cms/faq.ts
export interface CmsFaq {
  id: string
  question: string
  answer: string
  isActive: boolean
  order: number
  createdAt: Date | null
  updatedAt: Date | null
}
```

- [ ] **Step 2: Verificar manualmente**

Run: `npx vue-tsc --noEmit --project .nuxt/tsconfig.json` (ejecutar `npx
nuxi prepare` antes si hace falta). Expected: sin errores nuevos (los 2
preexistentes en `app.vue`/`error.vue` no cuentan).

---

### Task 3: Servicio `faq.service.ts`

**Files:**
- Create: `app/services/cms/faq.service.ts`

- [ ] **Step 1: Escribir el servicio**

```typescript
// app/services/cms/faq.service.ts
import {
  getFirestore,
  collection,
  getDocs,
  doc,
  addDoc,
  updateDoc,
  deleteDoc,
  orderBy,
  query,
  serverTimestamp,
  Timestamp,
} from 'firebase/firestore'
import type { CmsFaq } from '~/types/cms/faq'

function toDateOrNull(value: unknown): Date | null {
  return value instanceof Timestamp ? value.toDate() : null
}

export async function getFaqs(): Promise<CmsFaq[]> {
  const db = getFirestore()
  const q = query(collection(db, 'faq'), orderBy('order'))
  const snap = await getDocs(q)
  return snap.docs.map((d) => {
    const data = d.data()
    return {
      id: d.id,
      question: data.question ?? '',
      answer: data.answer ?? '',
      isActive: data.isActive === true,
      order: data.order ?? 0,
      createdAt: toDateOrNull(data.createdAt),
      updatedAt: toDateOrNull(data.updatedAt),
    }
  })
}

export async function createFaq(question: string, answer: string): Promise<void> {
  const db = getFirestore()
  await addDoc(collection(db, 'faq'), {
    question,
    answer,
    isActive: true,
    order: Date.now(),
    createdAt: serverTimestamp(),
    updatedAt: serverTimestamp(),
  })
}

export async function updateFaq(id: string, question: string, answer: string): Promise<void> {
  const db = getFirestore()
  await updateDoc(doc(db, 'faq', id), {
    question,
    answer,
    updatedAt: serverTimestamp(),
  })
}

export async function deleteFaq(id: string): Promise<void> {
  const db = getFirestore()
  await deleteDoc(doc(db, 'faq', id))
}

export async function toggleFaqActive(id: string, isActive: boolean): Promise<void> {
  const db = getFirestore()
  await updateDoc(doc(db, 'faq', id), { isActive, updatedAt: serverTimestamp() })
}
```

- [ ] **Step 2: Verificar manualmente**

Typecheck (mismo comando que Task 2). Sin errores nuevos.

---

### Task 4: Store `faq.store.ts`

**Files:**
- Create: `app/stores/cms/faq.store.ts`

- [ ] **Step 1: Escribir el store**

```typescript
// app/stores/cms/faq.store.ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import type { CmsFaq } from '~/types/cms/faq'
import {
  getFaqs,
  createFaq,
  updateFaq,
  deleteFaq,
  toggleFaqActive,
} from '~/services/cms/faq.service'

export const useCmsFaqStore = defineStore('cmsFaq', () => {
  const faqs = ref<CmsFaq[]>([])
  const loading = ref(false)
  const error = ref<string | null>(null)

  async function fetchFaqs(): Promise<void> {
    loading.value = true
    error.value = null
    try {
      faqs.value = await getFaqs()
    } catch {
      error.value = 'No se pudieron cargar las preguntas frecuentes.'
    } finally {
      loading.value = false
    }
  }

  async function saveFaq(question: string, answer: string, id?: string): Promise<boolean> {
    try {
      if (id) {
        await updateFaq(id, question, answer)
      } else {
        await createFaq(question, answer)
      }
      await fetchFaqs()
      return true
    } catch {
      return false
    }
  }

  async function removeFaq(id: string): Promise<boolean> {
    try {
      await deleteFaq(id)
      faqs.value = faqs.value.filter((f) => f.id !== id)
      return true
    } catch {
      return false
    }
  }

  async function toggleActive(id: string, isActive: boolean): Promise<boolean> {
    try {
      await toggleFaqActive(id, isActive)
      const faq = faqs.value.find((f) => f.id === id)
      if (faq) faq.isActive = isActive
      return true
    } catch {
      return false
    }
  }

  return {
    faqs,
    loading,
    error,
    fetchFaqs,
    saveFaq,
    removeFaq,
    toggleActive,
  }
})
```

- [ ] **Step 2: Verificar manualmente**

Typecheck. Sin errores nuevos.

---

### Task 5: Componente `FaqRow.vue`

**Files:**
- Create: `app/components/cms/faq/FaqRow.vue`

- [ ] **Step 1: Escribir el componente**

```vue
<script setup lang="ts">
import { Pencil, Trash2 } from 'lucide-vue-next'
import type { CmsFaq } from '~/types/cms/faq'

defineProps<{
  faq: CmsFaq
}>()

const emit = defineEmits<{
  edit: []
  delete: []
  toggle: [isActive: boolean]
}>()
</script>

<template>
  <div class="flex items-center justify-between gap-4 border-b border-forge-divider px-4 py-3 text-sm last:border-b-0">
    <div class="min-w-0 flex-1">
      <p class="truncate font-medium text-forge-text">{{ faq.question }}</p>
      <p class="truncate text-xs text-forge-muted">{{ faq.answer }}</p>
    </div>

    <button
      type="button"
      class="shrink-0 rounded px-2 py-1 text-[10px] uppercase tracking-wide"
      :class="faq.isActive
        ? 'bg-forge-success/10 text-forge-success'
        : 'bg-forge-surfaceAlt text-forge-muted'"
      @click="emit('toggle', !faq.isActive)"
    >
      {{ faq.isActive ? 'Activa' : 'Inactiva' }}
    </button>

    <div class="flex shrink-0 items-center gap-1">
      <button
        type="button"
        class="rounded p-2 text-forge-textSec hover:bg-forge-surfaceAlt hover:text-forge-text"
        aria-label="Editar"
        @click="emit('edit')"
      >
        <Pencil class="h-4 w-4" />
      </button>
      <button
        type="button"
        class="rounded p-2 text-forge-textSec hover:bg-forge-danger/10 hover:text-forge-danger"
        aria-label="Eliminar"
        @click="emit('delete')"
      >
        <Trash2 class="h-4 w-4" />
      </button>
    </div>
  </div>
</template>
```

- [ ] **Step 2: Verificar manualmente**

Typecheck. Sin errores nuevos.

---

### Task 6: Componente `FaqFormModal.vue`

**Files:**
- Create: `app/components/cms/faq/FaqFormModal.vue`

- [ ] **Step 1: Escribir el componente**

```vue
<script setup lang="ts">
import { ref, watch } from 'vue'
import { useCmsFaqStore } from '~/stores/cms/faq.store'
import type { CmsFaq } from '~/types/cms/faq'

const props = defineProps<{
  open: boolean
  editingFaq: CmsFaq | null
}>()

const emit = defineEmits<{
  close: []
}>()

const faqStore = useCmsFaqStore()

const question = ref('')
const answer = ref('')
const loading = ref(false)
const errorMessage = ref<string | null>(null)

watch(
  () => props.open,
  (isOpen) => {
    if (isOpen) {
      question.value = props.editingFaq?.question ?? ''
      answer.value = props.editingFaq?.answer ?? ''
      errorMessage.value = null
    }
  },
)

async function handleSubmit() {
  if (!question.value.trim() || !answer.value.trim()) return
  loading.value = true
  errorMessage.value = null
  const ok = await faqStore.saveFaq(question.value.trim(), answer.value.trim(), props.editingFaq?.id)
  loading.value = false
  if (ok) {
    emit('close')
  } else {
    errorMessage.value = 'No se pudo guardar la pregunta frecuente.'
  }
}
</script>

<template>
  <div v-if="open" class="fixed inset-0 z-50 flex items-center justify-center bg-black/60 px-4">
    <div class="w-full max-w-lg rounded-2xl border border-forge-divider bg-forge-surface p-6">
      <h2 class="text-lg font-semibold text-forge-text">
        {{ editingFaq ? 'Editar pregunta frecuente' : 'Nueva pregunta frecuente' }}
      </h2>

      <div class="mt-4 space-y-4">
        <div>
          <label for="faq-question" class="mb-1.5 block text-xs font-medium text-forge-textSec">
            Pregunta
          </label>
          <textarea
            id="faq-question"
            v-model="question"
            rows="2"
            class="w-full resize-none rounded-lg border border-forge-divider bg-forge-surfaceAlt px-3 py-2 text-sm text-forge-text focus:outline-none focus:ring-2 focus:ring-forge-primary"
          />
        </div>
        <div>
          <label for="faq-answer" class="mb-1.5 block text-xs font-medium text-forge-textSec">
            Respuesta
          </label>
          <textarea
            id="faq-answer"
            v-model="answer"
            rows="4"
            class="w-full resize-none rounded-lg border border-forge-divider bg-forge-surfaceAlt px-3 py-2 text-sm text-forge-text focus:outline-none focus:ring-2 focus:ring-forge-primary"
          />
        </div>
      </div>

      <p v-if="errorMessage" class="mt-4 text-sm text-forge-danger">
        {{ errorMessage }}
      </p>

      <div class="mt-6 flex justify-end gap-3">
        <button
          type="button"
          class="rounded-lg px-4 py-2 text-sm text-forge-textSec hover:bg-forge-surfaceAlt"
          @click="emit('close')"
        >
          Cancelar
        </button>
        <button
          type="button"
          :disabled="loading || !question.trim() || !answer.trim()"
          class="rounded-lg bg-forge-primary px-4 py-2 text-sm font-semibold text-white hover:bg-forge-accent disabled:opacity-60"
          @click="handleSubmit"
        >
          {{ loading ? 'Guardando…' : 'Guardar' }}
        </button>
      </div>
    </div>
  </div>
</template>
```

- [ ] **Step 2: Verificar manualmente**

Typecheck. Sin errores nuevos.

---

### Task 7: Página `/cms/faq` + activar en sidebar

**Files:**
- Create: `app/pages/cms/faq/index.vue`
- Modify: `app/components/cms/layout/CmsSidebar.vue`

- [ ] **Step 1: Escribir la página**

```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { Plus } from 'lucide-vue-next'
import { useCmsFaqStore } from '~/stores/cms/faq.store'
import FaqRow from '~/components/cms/faq/FaqRow.vue'
import FaqFormModal from '~/components/cms/faq/FaqFormModal.vue'
import ConfirmModal from '~/components/cms/shared/ConfirmModal.vue'
import EmptyState from '~/components/cms/shared/EmptyState.vue'
import type { CmsFaq } from '~/types/cms/faq'

definePageMeta({ layout: 'cms' })

const faqStore = useCmsFaqStore()

const showFormModal = ref(false)
const editingFaq = ref<CmsFaq | null>(null)

const showDeleteConfirm = ref(false)
const deletingFaq = ref<CmsFaq | null>(null)
const deleting = ref(false)

onMounted(() => {
  faqStore.fetchFaqs()
})

function openCreateModal() {
  editingFaq.value = null
  showFormModal.value = true
}

function openEditModal(faq: CmsFaq) {
  editingFaq.value = faq
  showFormModal.value = true
}

function openDeleteConfirm(faq: CmsFaq) {
  deletingFaq.value = faq
  showDeleteConfirm.value = true
}

async function handleDelete() {
  if (!deletingFaq.value) return
  deleting.value = true
  await faqStore.removeFaq(deletingFaq.value.id)
  deleting.value = false
  showDeleteConfirm.value = false
  deletingFaq.value = null
}
</script>

<template>
  <div>
    <div class="mb-6 flex items-center justify-between">
      <h1 class="text-xl font-bold text-forge-text">Preguntas frecuentes</h1>
      <button
        type="button"
        class="flex items-center gap-2 rounded-lg bg-forge-primary px-4 py-2 text-sm font-semibold text-white hover:bg-forge-accent"
        @click="openCreateModal"
      >
        <Plus class="h-4 w-4" />
        Nueva pregunta
      </button>
    </div>

    <EmptyState
      v-if="faqStore.error"
      title="No se pudieron cargar las preguntas frecuentes"
      :description="faqStore.error"
    />

    <div v-else-if="faqStore.loading" class="text-sm text-forge-muted">
      Cargando…
    </div>

    <EmptyState
      v-else-if="faqStore.faqs.length === 0"
      title="Todavía no hay preguntas frecuentes"
      description="Crea la primera con el botón de arriba."
    />

    <div v-else class="overflow-hidden rounded-xl border border-forge-divider">
      <FaqRow
        v-for="faq in faqStore.faqs"
        :key="faq.id"
        :faq="faq"
        @edit="openEditModal(faq)"
        @delete="openDeleteConfirm(faq)"
        @toggle="(isActive) => faqStore.toggleActive(faq.id, isActive)"
      />
    </div>

    <FaqFormModal
      :open="showFormModal"
      :editing-faq="editingFaq"
      @close="showFormModal = false"
    />

    <ConfirmModal
      :open="showDeleteConfirm"
      title="Eliminar pregunta frecuente"
      :message="`¿Seguro que quieres eliminar «${deletingFaq?.question ?? ''}»? Esta acción no se puede deshacer.`"
      confirm-label="Eliminar"
      :loading="deleting"
      @confirm="handleDelete"
      @cancel="showDeleteConfirm = false"
    />
  </div>
</template>
```

- [ ] **Step 2: Activar "FAQ" en el sidebar**

En `app/components/cms/layout/CmsSidebar.vue`, quitar "FAQ" del array
`comingSoon`:

```typescript
const comingSoon = [
  { label: 'Ejercicios', icon: Dumbbell },
  { label: 'Legal', icon: FileText },
  { label: 'Moderación', icon: ShieldAlert },
  { label: 'Notificaciones', icon: Bell },
  { label: 'Configuración', icon: Settings },
]
```

Y añadir un `NuxtLink` real, justo después del de "Usuarios":

```vue
      <NuxtLink
        to="/cms/faq"
        class="flex items-center gap-3 rounded-lg px-3 py-2 text-sm font-medium text-forge-textSec transition-all duration-150 ease-out hover:bg-forge-surfaceAlt hover:text-forge-text"
        active-class="!bg-forge-primary/10 !text-forge-primary border-l-2 border-forge-primary"
      >
        <HelpCircle class="h-4 w-4" />
        FAQ
      </NuxtLink>
```

(El import de `HelpCircle` desde `lucide-vue-next` ya existe en el archivo.)

- [ ] **Step 3: Verificar manualmente**

Typecheck + `npm run dev` + `curl http://localhost:3000/cms/faq` → 200, sin
errores nuevos en el log. Matar el proceso dev al terminar.

---

### Task 8: Verificación funcional CMS + commit

**Files:** ninguno nuevo — solo verificación y commit.

- [ ] **Step 1: Build completo**

Run: `npm run generate`. Expected: sin errores, prerenderiza `/cms/faq`
correctamente (shell `ssr:false`, ya cubierto por la routeRule `/cms/**`
existente).

- [ ] **Step 2: Limpiar artefactos de build regenerados**

Run: `git checkout -- .nuxt/ .output/ node_modules/ package-lock.json`
(estos quedan trackeados contra su propio `.gitignore` por un estado
preexistente del repo — no forman parte de este cambio).

- [ ] **Step 3: Recorrido manual (navegador, `npm run dev`)**

1. Sidebar → "FAQ" ya no dice "Próximamente".
2. `/cms/faq` vacío al principio → `EmptyState` "Todavía no hay preguntas
   frecuentes".
3. Crear una FAQ de prueba → aparece en la lista.
4. Editarla → cambios se reflejan.
5. Toggle activa/inactiva → cambia visualmente sin confirmación.
6. Eliminar → pide confirmación, luego desaparece de la lista.
7. Confirmar en Firestore (consola o `curl` a la REST API) que los cambios
   son reales.

- [ ] **Step 4: Commit**

```bash
git add app/types/cms/faq.ts app/services/cms/faq.service.ts app/stores/cms/faq.store.ts app/components/cms/faq/ app/pages/cms/faq/ app/components/cms/layout/CmsSidebar.vue
git commit -m "$(cat <<'EOF'
feat: añadir módulo FAQ al CMS

Listado, creación, edición, activar/desactivar y borrado de preguntas
frecuentes sobre la colección faq de Firestore (lectura pública, escritura
solo admin).
EOF
)"
```

No incluir en el commit: `docs/superpowers/specs/2026-08-08-faq-design.md`
ni `docs/superpowers/plans/2026-08-08-faq.md` si el usuario prefiere
commitearlos aparte — si no dice nada, inclúyelos también (son parte del
mismo trabajo).

---

### Task 9: Entity `FaqEntity` (Flutter)

**Files:**
- Create: `/Users/cris/Desktop/forge/lib/features/faq/domain/entities/faq_entity.dart`

- [ ] **Step 1: Escribir la entity**

Mismo estilo que `UserGoalEntity` (clase inmutable, `const` constructor):

```dart
class FaqEntity {
  final String id;
  final String question;
  final String answer;

  const FaqEntity({
    required this.id,
    required this.question,
    required this.answer,
  });
}
```

- [ ] **Step 2: Verificar manualmente**

Run (desde `/Users/cris/Desktop/forge`): `flutter analyze
lib/features/faq/domain/entities/faq_entity.dart`. Expected: sin errores.

---

### Task 10: Repository interface (Flutter)

**Files:**
- Create: `/Users/cris/Desktop/forge/lib/features/faq/domain/repositories/faq_repository.dart`

- [ ] **Step 1: Escribir la interfaz**

```dart
import '../entities/faq_entity.dart';

abstract class FaqRepository {
  Future<List<FaqEntity>> fetchActiveFaqs();
}
```

- [ ] **Step 2: Verificar manualmente**

`flutter analyze lib/features/faq/domain/repositories/faq_repository.dart`.
Sin errores.

---

### Task 11: Datasource Firestore (Flutter)

**Files:**
- Create: `/Users/cris/Desktop/forge/lib/features/faq/data/datasources/faq_firestore_datasource.dart`

- [ ] **Step 1: Escribir el datasource**

Mismo estilo que `GoalFirestoreDatasource` (constructor con `FirebaseFirestore?`
opcional, métodos privados de serialización):

```dart
import 'package:cloud_firestore/cloud_firestore.dart';

import '../../domain/entities/faq_entity.dart';

class FaqFirestoreDatasource {
  FaqFirestoreDatasource({FirebaseFirestore? firestore})
      : _db = firestore ?? FirebaseFirestore.instance;

  final FirebaseFirestore _db;

  Future<List<FaqEntity>> fetchActiveFaqs() async {
    final snap = await _db
        .collection('faq')
        .where('isActive', isEqualTo: true)
        .orderBy('order')
        .get();
    return snap.docs.map((d) => _fromMap(d.data(), d.id)).toList();
  }

  FaqEntity _fromMap(Map<String, dynamic> m, String id) {
    return FaqEntity(
      id: id,
      question: m['question'] as String? ?? '',
      answer: m['answer'] as String? ?? '',
    );
  }
}
```

- [ ] **Step 2: Verificar manualmente**

`flutter analyze
lib/features/faq/data/datasources/faq_firestore_datasource.dart`. Sin
errores.

---

### Task 12: Repository implementation (Flutter)

**Files:**
- Create: `/Users/cris/Desktop/forge/lib/features/faq/data/repositories/faq_repository_impl.dart`

- [ ] **Step 1: Escribir la implementación**

```dart
import '../../domain/entities/faq_entity.dart';
import '../../domain/repositories/faq_repository.dart';
import '../datasources/faq_firestore_datasource.dart';

class FaqRepositoryImpl implements FaqRepository {
  FaqRepositoryImpl(this._ds);
  final FaqFirestoreDatasource _ds;

  @override
  Future<List<FaqEntity>> fetchActiveFaqs() => _ds.fetchActiveFaqs();
}
```

- [ ] **Step 2: Verificar manualmente**

`flutter analyze
lib/features/faq/data/repositories/faq_repository_impl.dart`. Sin errores.

---

### Task 13: Providers Riverpod (Flutter)

**Files:**
- Create: `/Users/cris/Desktop/forge/lib/features/faq/presentation/providers/faq_providers.dart`

- [ ] **Step 1: Escribir los providers**

Mismo estilo que `goal_providers.dart`, pero sin depender de
`authStateChangesProvider` (lectura pública, no requiere sesión):

```dart
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../data/datasources/faq_firestore_datasource.dart';
import '../../data/repositories/faq_repository_impl.dart';
import '../../domain/entities/faq_entity.dart';
import '../../domain/repositories/faq_repository.dart';

final _firestoreProvider =
    Provider<FirebaseFirestore>((ref) => FirebaseFirestore.instance);

final faqDatasourceProvider = Provider<FaqFirestoreDatasource>(
  (ref) => FaqFirestoreDatasource(firestore: ref.watch(_firestoreProvider)),
);

final faqRepositoryProvider = Provider<FaqRepository>(
  (ref) => FaqRepositoryImpl(ref.watch(faqDatasourceProvider)),
);

final faqListProvider = FutureProvider.autoDispose<List<FaqEntity>>((ref) {
  return ref.watch(faqRepositoryProvider).fetchActiveFaqs();
});
```

- [ ] **Step 2: Verificar manualmente**

`flutter analyze
lib/features/faq/presentation/providers/faq_providers.dart`. Sin errores.

---

### Task 14: Actualizar `FaqScreen` para leer del provider

**Files:**
- Modify: `/Users/cris/Desktop/forge/lib/features/profile/presentation/screens/faq_screen.dart`

- [ ] **Step 1: Reescribir la pantalla**

Convertir de `StatefulWidget` a `ConsumerStatefulWidget` (se mantiene el
estado local `_expanded` para el acordeón — eso no cambia), y sustituir la
lista fija `items` por el contenido de `faqListProvider`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_animate/flutter_animate.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../../../core/theme/app_colors_theme.dart';
import '../../../../l10n/app_localizations.dart';
import '../../../faq/domain/entities/faq_entity.dart';
import '../../../faq/presentation/providers/faq_providers.dart';

class FaqScreen extends ConsumerStatefulWidget {
  const FaqScreen({super.key});

  @override
  ConsumerState<FaqScreen> createState() => _FaqScreenState();
}

class _FaqScreenState extends ConsumerState<FaqScreen> {
  final _expanded = <int>{};

  void _toggle(int index) {
    setState(() {
      if (_expanded.contains(index)) {
        _expanded.remove(index);
      } else {
        _expanded.add(index);
      }
    });
  }

  @override
  Widget build(BuildContext context) {
    final c = AppColorsTheme.of(context);
    final l10n = AppLocalizations.of(context)!;
    final faqAsync = ref.watch(faqListProvider);

    return Scaffold(
      backgroundColor: c.background,
      appBar: AppBar(
        backgroundColor: c.background,
        elevation: 0,
        surfaceTintColor: Colors.transparent,
        iconTheme: IconThemeData(color: c.onBackground),
        title: Text(
          l10n.faqTitle,
          style: TextStyle(
            color: c.onBackground,
            fontWeight: FontWeight.w800,
            fontSize: 18,
            letterSpacing: -0.5,
          ),
        ),
      ),
      body: SafeArea(
        child: faqAsync.when(
          loading: () => const Center(child: CircularProgressIndicator()),
          error: (error, stackTrace) => Center(
            child: Column(
              mainAxisSize: MainAxisSize.min,
              children: [
                Text(
                  'No se pudieron cargar las preguntas frecuentes.',
                  style: TextStyle(color: c.hint),
                ),
                const SizedBox(height: 12),
                TextButton(
                  onPressed: () => ref.invalidate(faqListProvider),
                  child: const Text('Reintentar'),
                ),
              ],
            ),
          ),
          data: (items) {
            if (items.isEmpty) {
              return Center(
                child: Text(
                  'No hay preguntas frecuentes disponibles.',
                  style: TextStyle(color: c.hint),
                ),
              );
            }
            return ListView.separated(
              padding: const EdgeInsets.all(16),
              itemCount: items.length,
              separatorBuilder: (_, i) => const SizedBox(height: 10),
              itemBuilder: (ctx, i) {
                final FaqEntity item = items[i];
                final open = _expanded.contains(i);
                return _FaqCard(
                  question: item.question,
                  answer: item.answer,
                  isOpen: open,
                  onTap: () => _toggle(i),
                  delay: i * 80,
                );
              },
            );
          },
        ),
      ),
    );
  }
}
```

**`_FaqCard` no cambia** — sigue exactamente igual que el código actual
(líneas 77-194 del archivo original), no la reescribas, solo mantenla tal
cual está debajo de la clase `_FaqScreenState` de arriba.

- [ ] **Step 2: Verificar manualmente**

Run: `flutter analyze
lib/features/profile/presentation/screens/faq_screen.dart`. Sin errores. Si
aparece un error de tipo "unused import" para `AppLocalizations` u otro,
revisar que `l10n.faqTitle` siga usándose (si no, es una señal de que el
Step 1 se aplicó mal — `faqTitle` se mantiene, solo cambia el contenido de
las preguntas).

---

### Task 15: Limpiar claves de contenido en los `.arb`

**Files:**
- Modify: `/Users/cris/Desktop/forge/lib/l10n/app_es.arb:232-237`
- Modify: `/Users/cris/Desktop/forge/lib/l10n/app_en.arb:232-237`

- [ ] **Step 1: Eliminar las 6 claves de contenido (no `faqTitle`)**

En `app_es.arb`, eliminar estas 6 líneas (232-237), dejando `faqTitle`
intacto:

```json
  "faqQ1": "¿Cómo registro un entrenamiento?",
  "faqA1": "Ve al apartado de Entrenamientos, selecciona o crea una rutina y pulsa \"Iniciar\". Cuando termines, pulsa \"Finalizar\" para guardar automáticamente tus series, pesos y repeticiones.",
  "faqQ2": "¿Puedo crear mis propias rutinas?",
  "faqA2": "Sí. En la pantalla de Entrenamientos pulsa el botón \"+\", añade los ejercicios que quieras con sus series y repeticiones y guarda la rutina. Podrás editarla en cualquier momento.",
  "faqQ3": "¿Cómo funcionan los puntos de experiencia (XP)?",
  "faqA3": "Ganas XP al completar entrenamientos, superar récords personales y mantener tu racha de días activos. El XP acumulado sube tu nivel, desbloqueando nuevas insignias en tu perfil.",
```

En `app_en.arb`, eliminar el mismo rango de líneas equivalentes (232-237):

```json
  "faqQ1": "How do I log a workout?",
  "faqA1": "Go to the Workouts section, select or create a routine and tap \"Start\". When you're done, tap \"Finish\" to automatically save your sets, weights and reps.",
  "faqQ2": "Can I create my own routines?",
  "faqA2": "Yes. On the Workouts screen tap the \"+\" button, add the exercises you want with their sets and reps, and save the routine. You can edit it at any time.",
  "faqQ3": "How does experience (XP) work?",
  "faqA3": "You earn XP by completing workouts, breaking personal records and keeping your active-days streak. Accumulated XP raises your level and unlocks new badges on your profile.",
```

Verificar tras el borrado que el JSON sigue siendo válido (sin coma colgante
al final del bloque anterior ni al principio del siguiente —
`settingsSupportContactUs` antes, `settingsSectionAbout` después, ambos
deben seguir con coma excepto si uno de los dos queda como último campo del
objeto, lo cual no es el caso aquí).

- [ ] **Step 2: Regenerar las localizations**

Run (desde `/Users/cris/Desktop/forge`): `flutter gen-l10n`. Expected:
completa sin errores; `lib/l10n/app_localizations_es.dart`,
`app_localizations_en.dart` y `app_localizations.dart` ya no declaran
`faqQ1`/`faqA1`/etc.

---

### Task 16: Verificación final Flutter

**Files:** ninguno — solo verificación.

- [ ] **Step 1: Analyze completo**

Run (desde `/Users/cris/Desktop/forge`): `flutter analyze`. Expected: sin
errores nuevos respecto al estado antes de este plan (si ya había warnings
preexistentes ajenos, no cuentan; cualquier error en los archivos tocados
por este plan sí).

- [ ] **Step 2: Recorrido manual**

Con la app corriendo en un dispositivo/emulador (`flutter run`):
1. Navegar a Perfil → Ajustes → Soporte → FAQ.
2. Con la colección `faq` vacía: mensaje "No hay preguntas frecuentes
   disponibles."
3. Tras crear preguntas desde el CMS (Task 8 ya verificada): aparecen aquí,
   mismo diseño visual que antes (tarjetas expandibles, animación de
   entrada).
4. Desactivar una FAQ desde el CMS → tras recargar esta pantalla (o hacer
   pull-to-refresh si existiera; si no, basta con re-navegar), esa FAQ ya
   no aparece.
5. Sin red (modo avión): estado de error con botón "Reintentar" visible.

No se crea ningún commit en `forge` — el trabajo queda en el working tree
para que el usuario lo revise y decida.
