# Legal (Privacidad + Términos) — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Colección `legal_documents` (dos IDs fijos: `privacy_policy`, `terms_of_service`) editable desde el CMS, visible públicamente en `forge_web` (`/legal/privacidad`, `/legal/terminos`) y en la app móvil (nueva sección "Legal" en Ajustes).

**Architecture:** CMS — `services → stores → components/pages`, igual que FAQ/Usuarios. Portada pública — páginas Nuxt sin login con el layout `default` ya existente. App móvil — Clean Architecture + Riverpod, mismo patrón que `features/faq`, con `flutter_html` (nueva dependencia) para renderizar el HTML.

**Tech Stack:** Nuxt 4/Pinia/Firebase JS SDK (CMS + portada). Flutter/Riverpod/`cloud_firestore`/`flutter_html` (app — única dependencia nueva en todo el plan).

**Spec:** `docs/superpowers/specs/2026-08-08-legal-design.md`

---

## Nota sobre este plan

- Commits en `forge_web`: autorizados, uno al final de la sección CMS+portada
  (Task 9). Commits en `forge`: **no autorizados**, todo queda sin commitear.
- Deploy de `firestore.rules`: autorizado sin pausa de confirmación (dry-run
  primero, igual que en los planes anteriores).
- Sin tests en ningún repo.

---

### Task 1: Regla `legal_documents` en `forge/firestore.rules` + deploy

**Files:**
- Modify: `/Users/cris/Desktop/forge/firestore.rules`

- [ ] **Step 1**: Añadir, junto al bloque `faq` ya existente (mismo bloque
`match` de nivel superior), un bloque nuevo:

```javascript
    match /legal_documents/{docId} {
      allow read: if true;
      allow write: if isAdmin();
    }
```

- [ ] **Step 2**: `firebase deploy --only firestore:rules --dry-run` desde
`/Users/cris/Desktop/forge`. Expected: compila correctamente.

- [ ] **Step 3**: `firebase deploy --only firestore:rules`. Expected:
`Deploy complete!`.

- [ ] **Step 4**: Verificar lectura anónima (debe funcionar, lista vacía o
`404` de documento individual — la colección aún no tiene documentos):
```bash
curl -s "https://firestore.googleapis.com/v1/projects/gym-app-41fd6/databases/(default)/documents/legal_documents"
```
Expected: JSON sin `"code": 403`.

No se hace `git commit` de `firestore.rules`.

---

### Task 2: Tipos CMS (`CmsLegalDocument`)

**Files:**
- Create: `app/types/cms/legal.ts`

- [ ] **Step 1**:

```typescript
// app/types/cms/legal.ts
export type CmsLegalDocumentId = 'privacy_policy' | 'terms_of_service'

export interface CmsLegalDocument {
  id: CmsLegalDocumentId
  contentHtml: string
  version: number
  updatedAt: Date | null
}
```

- [ ] **Step 2**: Typecheck (`npx vue-tsc --noEmit --project .nuxt/tsconfig.json`,
tras `npx nuxi prepare` si hace falta). Sin errores nuevos.

---

### Task 3: Servicio `legal.service.ts`

**Files:**
- Create: `app/services/cms/legal.service.ts`

- [ ] **Step 1**:

```typescript
// app/services/cms/legal.service.ts
import { getFirestore, doc, getDoc, setDoc, serverTimestamp, Timestamp } from 'firebase/firestore'
import type { CmsLegalDocument, CmsLegalDocumentId } from '~/types/cms/legal'

const DOCUMENT_IDS: CmsLegalDocumentId[] = ['privacy_policy', 'terms_of_service']

function toDateOrNull(value: unknown): Date | null {
  return value instanceof Timestamp ? value.toDate() : null
}

export async function getLegalDocuments(): Promise<CmsLegalDocument[]> {
  const db = getFirestore()
  const results = await Promise.all(
    DOCUMENT_IDS.map(async (id) => {
      const snap = await getDoc(doc(db, 'legal_documents', id))
      const data = snap.data()
      return {
        id,
        contentHtml: data?.contentHtml ?? '',
        version: data?.version ?? 0,
        updatedAt: toDateOrNull(data?.updatedAt),
      }
    }),
  )
  return results
}

export async function saveLegalDocument(id: CmsLegalDocumentId, contentHtml: string, currentVersion: number): Promise<void> {
  const db = getFirestore()
  await setDoc(
    doc(db, 'legal_documents', id),
    {
      contentHtml,
      version: currentVersion + 1,
      updatedAt: serverTimestamp(),
    },
    { merge: true },
  )
}
```

- [ ] **Step 2**: Typecheck. Sin errores nuevos.

---

### Task 4: Store `legal.store.ts`

**Files:**
- Create: `app/stores/cms/legal.store.ts`

- [ ] **Step 1**:

```typescript
// app/stores/cms/legal.store.ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import type { CmsLegalDocument, CmsLegalDocumentId } from '~/types/cms/legal'
import { getLegalDocuments, saveLegalDocument } from '~/services/cms/legal.service'

export const useCmsLegalStore = defineStore('cmsLegal', () => {
  const documents = ref<CmsLegalDocument[]>([])
  const loading = ref(false)
  const error = ref<string | null>(null)

  async function fetchDocuments(): Promise<void> {
    loading.value = true
    error.value = null
    try {
      documents.value = await getLegalDocuments()
    } catch {
      error.value = 'No se pudieron cargar los documentos legales.'
    } finally {
      loading.value = false
    }
  }

  async function saveDocument(id: CmsLegalDocumentId, contentHtml: string): Promise<boolean> {
    const current = documents.value.find((d) => d.id === id)
    try {
      await saveLegalDocument(id, contentHtml, current?.version ?? 0)
      await fetchDocuments()
      return true
    } catch {
      return false
    }
  }

  return {
    documents,
    loading,
    error,
    fetchDocuments,
    saveDocument,
  }
})
```

- [ ] **Step 2**: Typecheck. Sin errores nuevos.

---

### Task 5: Componente `LegalDocumentCard.vue`

**Files:**
- Create: `app/components/cms/legal/LegalDocumentCard.vue`

- [ ] **Step 1**:

```vue
<script setup lang="ts">
import { Pencil } from 'lucide-vue-next'
import type { CmsLegalDocument } from '~/types/cms/legal'

defineProps<{
  document: CmsLegalDocument
  title: string
}>()

const emit = defineEmits<{
  edit: []
}>()
</script>

<template>
  <div class="rounded-xl border border-forge-divider bg-forge-surface p-5">
    <div class="flex items-start justify-between gap-4">
      <div>
        <h2 class="font-semibold text-forge-text">{{ title }}</h2>
        <p class="mt-1 text-xs text-forge-muted">
          <template v-if="document.contentHtml">
            Versión {{ document.version }} · actualizado el
            {{ document.updatedAt ? document.updatedAt.toLocaleDateString('es-ES') : '—' }}
          </template>
          <template v-else>
            Aún no publicado
          </template>
        </p>
      </div>
      <button
        type="button"
        class="flex items-center gap-2 rounded-lg border border-forge-divider px-3 py-1.5 text-sm text-forge-textSec hover:bg-forge-surfaceAlt hover:text-forge-text"
        @click="emit('edit')"
      >
        <Pencil class="h-4 w-4" />
        Editar
      </button>
    </div>
  </div>
</template>
```

- [ ] **Step 2**: Typecheck. Sin errores nuevos.

---

### Task 6: Componente `LegalFormModal.vue`

**Files:**
- Create: `app/components/cms/legal/LegalFormModal.vue`

- [ ] **Step 1**:

```vue
<script setup lang="ts">
import { ref, watch } from 'vue'
import { useCmsLegalStore } from '~/stores/cms/legal.store'
import type { CmsLegalDocument, CmsLegalDocumentId } from '~/types/cms/legal'

const props = defineProps<{
  open: boolean
  title: string
  document: CmsLegalDocument | null
}>()

const emit = defineEmits<{
  close: []
}>()

const legalStore = useCmsLegalStore()

const contentHtml = ref('')
const loading = ref(false)
const errorMessage = ref<string | null>(null)

watch(
  () => props.open,
  (isOpen) => {
    if (isOpen) {
      contentHtml.value = props.document?.contentHtml ?? ''
      errorMessage.value = null
    }
  },
)

async function handleSubmit() {
  if (!props.document || !contentHtml.value.trim()) return
  loading.value = true
  errorMessage.value = null
  const ok = await legalStore.saveDocument(props.document.id as CmsLegalDocumentId, contentHtml.value.trim())
  loading.value = false
  if (ok) {
    emit('close')
  } else {
    errorMessage.value = 'No se pudo guardar el documento.'
  }
}
</script>

<template>
  <div v-if="open" class="fixed inset-0 z-50 flex items-center justify-center bg-black/60 px-4">
    <div class="w-full max-w-2xl rounded-2xl border border-forge-divider bg-forge-surface p-6">
      <h2 class="text-lg font-semibold text-forge-text">Editar {{ title }}</h2>
      <p class="mt-2 text-xs text-forge-muted">
        Pega aquí el HTML del documento (títulos <code>&lt;h2&gt;</code>/<code>&lt;h3&gt;</code>,
        párrafos <code>&lt;p&gt;</code>, listas <code>&lt;ul&gt;/&lt;li&gt;</code>,
        <code>&lt;strong&gt;</code>). Se muestra tal cual, sin editor visual de por medio.
      </p>

      <textarea
        v-model="contentHtml"
        rows="16"
        class="mt-4 w-full resize-none rounded-lg border border-forge-divider bg-forge-surfaceAlt px-3 py-2 font-mono text-xs text-forge-text focus:outline-none focus:ring-2 focus:ring-forge-primary"
      />

      <p v-if="errorMessage" class="mt-4 text-sm text-forge-danger">
        {{ errorMessage }}
      </p>

      <div class="mt-6 flex justify-end gap-3">
        <button
          type="button"
          class="rounded-lg px-4 py-2 text-sm text-forge-textSec hover:bg-forge-surfaceAlt"
          @click="emit('close')"
        >
          Cancelar
        </button>
        <button
          type="button"
          :disabled="loading || !contentHtml.trim()"
          class="rounded-lg bg-forge-primary px-4 py-2 text-sm font-semibold text-white hover:bg-forge-accent disabled:opacity-60"
          @click="handleSubmit"
        >
          {{ loading ? 'Publicando…' : 'Publicar cambios' }}
        </button>
      </div>
    </div>
  </div>
</template>
```

- [ ] **Step 2**: Typecheck. Sin errores nuevos.

---

### Task 7: Página `/cms/legal` + activar en sidebar

**Files:**
- Create: `app/pages/cms/legal/index.vue`
- Modify: `app/components/cms/layout/CmsSidebar.vue`

- [ ] **Step 1**: Escribir la página.

```vue
<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { useCmsLegalStore } from '~/stores/cms/legal.store'
import LegalDocumentCard from '~/components/cms/legal/LegalDocumentCard.vue'
import LegalFormModal from '~/components/cms/legal/LegalFormModal.vue'
import EmptyState from '~/components/cms/shared/EmptyState.vue'
import type { CmsLegalDocument, CmsLegalDocumentId } from '~/types/cms/legal'

definePageMeta({ layout: 'cms' })

const legalStore = useCmsLegalStore()

const TITLES: Record<CmsLegalDocumentId, string> = {
  privacy_policy: 'Política de privacidad',
  terms_of_service: 'Términos de servicio',
}

const showModal = ref(false)
const editingDocument = ref<CmsLegalDocument | null>(null)
const editingTitle = computed(() => editingDocument.value ? TITLES[editingDocument.value.id] : '')

onMounted(() => {
  legalStore.fetchDocuments()
})

function openEdit(document: CmsLegalDocument) {
  editingDocument.value = document
  showModal.value = true
}
</script>

<template>
  <div>
    <h1 class="mb-6 text-xl font-bold text-forge-text">Legal</h1>

    <EmptyState
      v-if="legalStore.error"
      title="No se pudieron cargar los documentos legales"
      :description="legalStore.error"
    />

    <div v-else-if="legalStore.loading" class="text-sm text-forge-muted">
      Cargando…
    </div>

    <div v-else class="space-y-4">
      <LegalDocumentCard
        v-for="document in legalStore.documents"
        :key="document.id"
        :document="document"
        :title="TITLES[document.id]"
        @edit="openEdit(document)"
      />
    </div>

    <LegalFormModal
      :open="showModal"
      :title="editingTitle"
      :document="editingDocument"
      @close="showModal = false"
    />
  </div>
</template>
```

- [ ] **Step 2**: Activar "Legal" en el sidebar. En
`app/components/cms/layout/CmsSidebar.vue`, quitar "Legal" de `comingSoon`:

```typescript
const comingSoon = [
  { label: 'Ejercicios', icon: Dumbbell },
  { label: 'Moderación', icon: ShieldAlert },
  { label: 'Notificaciones', icon: Bell },
  { label: 'Configuración', icon: Settings },
]
```

Y añadir un `NuxtLink` real, después del de "FAQ":

```vue
      <NuxtLink
        to="/cms/legal"
        class="flex items-center gap-3 rounded-lg px-3 py-2 text-sm font-medium text-forge-textSec transition-all duration-150 ease-out hover:bg-forge-surfaceAlt hover:text-forge-text"
        active-class="!bg-forge-primary/10 !text-forge-primary border-l-2 border-forge-primary"
      >
        <FileText class="h-4 w-4" />
        Legal
      </NuxtLink>
```

(El import de `FileText` desde `lucide-vue-next` ya existe en el archivo.)

- [ ] **Step 3**: Typecheck + `npm run dev` + `curl http://localhost:3000/cms/legal` → 200. Matar el proceso dev al terminar.

---

### Task 8: Páginas públicas + links en el footer

**Files:**
- Create: `app/pages/legal/privacidad.vue`
- Create: `app/pages/legal/terminos.vue`
- Modify: `app/components/AppFooter.vue`

- [ ] **Step 1**: `app/pages/legal/privacidad.vue`

```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { getFirestore, doc, getDoc } from 'firebase/firestore'

const contentHtml = ref('')
const loading = ref(true)
const errorMessage = ref<string | null>(null)

onMounted(async () => {
  try {
    const db = getFirestore()
    const snap = await getDoc(doc(db, 'legal_documents', 'privacy_policy'))
    contentHtml.value = snap.data()?.contentHtml ?? ''
  } catch {
    errorMessage.value = 'No se pudo cargar el documento.'
  } finally {
    loading.value = false
  }
})
</script>

<template>
  <div class="mx-auto max-w-2xl px-6 py-16">
    <h1 class="mb-8 text-2xl font-extrabold text-forge-text">Política de privacidad</h1>

    <p v-if="loading" class="text-sm text-forge-muted">Cargando…</p>
    <p v-else-if="errorMessage" class="text-sm text-forge-muted">{{ errorMessage }}</p>
    <p v-else-if="!contentHtml" class="text-sm text-forge-muted">
      Este documento aún no ha sido publicado.
    </p>
    <div v-else class="legal-content text-sm leading-relaxed text-forge-textSec" v-html="contentHtml" />
  </div>
</template>

<style scoped>
.legal-content :deep(h2) {
  margin-top: 2rem;
  margin-bottom: 0.75rem;
  font-size: 1.125rem;
  font-weight: 700;
  color: #EEEEEE;
}
.legal-content :deep(h2:first-child) {
  margin-top: 0;
}
.legal-content :deep(h3) {
  margin-top: 1.5rem;
  margin-bottom: 0.5rem;
  font-size: 1rem;
  font-weight: 700;
  color: #EEEEEE;
}
.legal-content :deep(p) {
  margin-bottom: 1rem;
}
.legal-content :deep(ul) {
  margin-bottom: 1rem;
  padding-left: 1.25rem;
  list-style: disc;
}
.legal-content :deep(li) {
  margin-bottom: 0.25rem;
}
.legal-content :deep(strong) {
  font-weight: 700;
  color: #EEEEEE;
}
</style>
```

- [ ] **Step 2**: `app/pages/legal/terminos.vue` — idéntico al anterior,
cambiando únicamente: título a "Términos de servicio", el `doc(db,
'legal_documents', 'privacy_policy')` a `doc(db, 'legal_documents',
'terms_of_service')`.

- [ ] **Step 3**: Añadir links en el footer. En
`app/components/AppFooter.vue`, dentro del "Bottom row" (la última `div`,
la que tiene el copyright), añadir un tercer elemento con los dos links,
antes del párrafo de copyright:

```vue
      <div class="flex flex-col sm:flex-row items-center justify-between gap-2">
        <div class="flex items-center gap-4">
          <p class="text-forge-muted text-xs">© 2026 Coded by Criss</p>
          <NuxtLink to="/legal/privacidad" class="text-forge-muted text-xs hover:text-forge-accent transition-colors duration-200">
            Política de privacidad
          </NuxtLink>
          <NuxtLink to="/legal/terminos" class="text-forge-muted text-xs hover:text-forge-accent transition-colors duration-200">
            Términos de servicio
          </NuxtLink>
        </div>
        <p class="text-forge-muted text-xs">Hecho con ❤️ para los que no se rinden</p>
      </div>
```

(Sustituye el `<p>` de copyright suelto que hay ahí hoy — envuélvelo en el
`<div>` nuevo junto a los dos links, tal como se muestra arriba.)

- [ ] **Step 4**: Typecheck + `npm run dev` + `curl` a `/legal/privacidad`,
`/legal/terminos` y `/` → todas 200. Matar el proceso dev al terminar.

---

### Task 9: Verificación funcional CMS + portada + commit

**Files:** ninguno nuevo.

- [ ] **Step 1**: `npm run generate`. Expected: sin errores, prerenderiza
`/cms/legal`, `/legal/privacidad`, `/legal/terminos`.

- [ ] **Step 2**: `git checkout -- .nuxt/ .output/ node_modules/
package-lock.json` si quedó algún artefacto de build modificado (ya no
deberían aparecer trackeados tras la limpieza previa, pero verificar con
`git status --short` de todas formas).

- [ ] **Step 3**: Recorrido manual (navegador, `npm run dev`):
1. `/cms/legal` → dos tarjetas, ambas "Aún no publicado".
2. Editar "Política de privacidad", pegar HTML de prueba con `<h2>`, `<p>`,
   `<ul><li>`, `<strong>` → guardar → tarjeta muestra "Versión 1 ·
   actualizado el [fecha]".
3. `/legal/privacidad` (pública, sin login) → muestra el HTML con formato
   correcto (títulos, párrafos, listas, negritas).
4. Footer de `/` → los dos links nuevos navegan correctamente.
5. Confirmar en Firestore que `legal_documents/privacy_policy` tiene
   `version: 1`.

- [ ] **Step 4**: Commit.

```bash
git add app/types/cms/legal.ts app/services/cms/legal.service.ts app/stores/cms/legal.store.ts app/components/cms/legal/ app/pages/cms/legal/ app/pages/legal/ app/components/cms/layout/CmsSidebar.vue app/components/AppFooter.vue
git commit -m "$(cat <<'EOF'
feat: añadir módulo Legal al CMS y páginas públicas de privacidad/términos

Edición de política de privacidad y términos de servicio desde el CMS
(HTML crudo, sin editor WYSIWYG), publicados en /legal/privacidad y
/legal/terminos sobre la colección legal_documents (lectura pública,
escritura solo admin).
EOF
)"
```

---

### Task 10: Añadir `flutter_html` a `pubspec.yaml`

**Files:**
- Modify: `/Users/cris/Desktop/forge/pubspec.yaml`

- [ ] **Step 1**: Añadir la dependencia junto a las demás (por ejemplo cerca
de `flutter_animate`), misma versión ya validada en producción en `Huby`:

```yaml
  flutter_html: ^3.0.0
```

- [ ] **Step 2**: `flutter pub get` desde `/Users/cris/Desktop/forge`.
Expected: resuelve sin conflictos de versiones.

---

### Task 11: Entity + enum (Flutter)

**Files:**
- Create: `/Users/cris/Desktop/forge/lib/features/legal/domain/entities/legal_document_entity.dart`

- [ ] **Step 1**:

```dart
enum LegalDocumentType {
  privacyPolicy('privacy_policy'),
  termsOfService('terms_of_service');

  final String firestoreId;

  const LegalDocumentType(this.firestoreId);
}

class LegalDocumentEntity {
  final String id;
  final String contentHtml;
  final int version;

  const LegalDocumentEntity({
    required this.id,
    required this.contentHtml,
    required this.version,
  });
}
```

- [ ] **Step 2**: `flutter analyze
lib/features/legal/domain/entities/legal_document_entity.dart`. Sin errores.

---

### Task 12: Repository interface (Flutter)

**Files:**
- Create: `/Users/cris/Desktop/forge/lib/features/legal/domain/repositories/legal_repository.dart`

- [ ] **Step 1**:

```dart
import '../entities/legal_document_entity.dart';

abstract class LegalRepository {
  Future<LegalDocumentEntity> fetchDocument(LegalDocumentType type);
}
```

- [ ] **Step 2**: `flutter analyze
lib/features/legal/domain/repositories/legal_repository.dart`. Sin errores.

---

### Task 13: Datasource Firestore (Flutter)

**Files:**
- Create: `/Users/cris/Desktop/forge/lib/features/legal/data/datasources/legal_firestore_datasource.dart`

- [ ] **Step 1**:

```dart
import 'package:cloud_firestore/cloud_firestore.dart';

import '../../domain/entities/legal_document_entity.dart';

class LegalFirestoreDatasource {
  LegalFirestoreDatasource({FirebaseFirestore? firestore})
      : _db = firestore ?? FirebaseFirestore.instance;

  final FirebaseFirestore _db;

  Future<LegalDocumentEntity> fetchDocument(LegalDocumentType type) async {
    final doc = await _db.collection('legal_documents').doc(type.firestoreId).get();
    final data = doc.data();
    return LegalDocumentEntity(
      id: type.firestoreId,
      contentHtml: data?['contentHtml'] as String? ?? '',
      version: (data?['version'] as num?)?.toInt() ?? 0,
    );
  }
}
```

- [ ] **Step 2**: `flutter analyze
lib/features/legal/data/datasources/legal_firestore_datasource.dart`. Sin
errores.

---

### Task 14: Repository implementation (Flutter)

**Files:**
- Create: `/Users/cris/Desktop/forge/lib/features/legal/data/repositories/legal_repository_impl.dart`

- [ ] **Step 1**:

```dart
import '../../domain/entities/legal_document_entity.dart';
import '../../domain/repositories/legal_repository.dart';
import '../datasources/legal_firestore_datasource.dart';

class LegalRepositoryImpl implements LegalRepository {
  LegalRepositoryImpl(this._ds);
  final LegalFirestoreDatasource _ds;

  @override
  Future<LegalDocumentEntity> fetchDocument(LegalDocumentType type) =>
      _ds.fetchDocument(type);
}
```

- [ ] **Step 2**: `flutter analyze
lib/features/legal/data/repositories/legal_repository_impl.dart`. Sin
errores.

---

### Task 15: Providers Riverpod (Flutter)

**Files:**
- Create: `/Users/cris/Desktop/forge/lib/features/legal/presentation/providers/legal_providers.dart`

- [ ] **Step 1**:

```dart
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../data/datasources/legal_firestore_datasource.dart';
import '../../data/repositories/legal_repository_impl.dart';
import '../../domain/entities/legal_document_entity.dart';
import '../../domain/repositories/legal_repository.dart';

final _firestoreProvider =
    Provider<FirebaseFirestore>((ref) => FirebaseFirestore.instance);

final legalDatasourceProvider = Provider<LegalFirestoreDatasource>(
  (ref) => LegalFirestoreDatasource(firestore: ref.watch(_firestoreProvider)),
);

final legalRepositoryProvider = Provider<LegalRepository>(
  (ref) => LegalRepositoryImpl(ref.watch(legalDatasourceProvider)),
);

final legalDocumentProvider = FutureProvider.autoDispose
    .family<LegalDocumentEntity, LegalDocumentType>((ref, type) {
  return ref.watch(legalRepositoryProvider).fetchDocument(type);
});
```

- [ ] **Step 2**: `flutter analyze
lib/features/legal/presentation/providers/legal_providers.dart`. Sin
errores.

---

### Task 16: Pantalla `LegalDocumentScreen` (Flutter)

**Files:**
- Create: `/Users/cris/Desktop/forge/lib/features/legal/presentation/screens/legal_document_screen.dart`

- [ ] **Step 1**: Mismo patrón ya probado en la app `Huby` (`Html()` de
`flutter_html`, estilo del `body` heredando color/tamaño del tema):

```dart
import 'package:flutter/material.dart';
import 'package:flutter_html/flutter_html.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../../../core/theme/app_colors_theme.dart';
import '../providers/legal_providers.dart';
import '../../domain/entities/legal_document_entity.dart';

class LegalDocumentScreen extends ConsumerWidget {
  const LegalDocumentScreen({
    super.key,
    required this.title,
    required this.documentType,
  });

  final String title;
  final LegalDocumentType documentType;

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final c = AppColorsTheme.of(context);
    final documentAsync = ref.watch(legalDocumentProvider(documentType));

    return Scaffold(
      backgroundColor: c.background,
      appBar: AppBar(
        backgroundColor: c.background,
        elevation: 0,
        surfaceTintColor: Colors.transparent,
        iconTheme: IconThemeData(color: c.onBackground),
        title: Text(
          title,
          style: TextStyle(
            color: c.onBackground,
            fontWeight: FontWeight.w800,
            fontSize: 18,
            letterSpacing: -0.5,
          ),
        ),
      ),
      body: SafeArea(
        child: documentAsync.when(
          loading: () => const Center(child: CircularProgressIndicator()),
          error: (error, stackTrace) => Center(
            child: Padding(
              padding: const EdgeInsets.symmetric(horizontal: 24),
              child: Text(
                'No se pudo cargar el documento. Inténtalo de nuevo más tarde.',
                textAlign: TextAlign.center,
                style: TextStyle(color: c.hint),
              ),
            ),
          ),
          data: (document) {
            if (document.contentHtml.isEmpty) {
              return Center(
                child: Text(
                  'Este documento aún no ha sido publicado.',
                  style: TextStyle(color: c.hint),
                ),
              );
            }
            return SingleChildScrollView(
              padding: const EdgeInsets.all(20),
              child: Html(
                data: document.contentHtml,
                style: {
                  'body': Style(
                    margin: Margins.zero,
                    padding: HtmlPaddings.zero,
                    color: c.onBackground,
                  ),
                },
              ),
            );
          },
        ),
      ),
    );
  }
}
```

(Ajustar el import de `AppColorsTheme` si su ruta relativa real difiere de
`../../../../core/theme/app_colors_theme.dart` — usar la misma que ya usa
`faq_screen.dart`, que vive en una carpeta con la misma profundidad relativa
a `core/`.)

- [ ] **Step 2**: `flutter analyze
lib/features/legal/presentation/screens/legal_document_screen.dart`. Sin
errores.

---

### Task 17: Nueva sección "Legal" en Ajustes + claves i18n

**Files:**
- Modify: `/Users/cris/Desktop/forge/lib/features/profile/presentation/screens/settings_screen.dart:240-243`
- Modify: `/Users/cris/Desktop/forge/lib/l10n/app_es.arb`
- Modify: `/Users/cris/Desktop/forge/lib/l10n/app_en.arb`

- [ ] **Step 1**: Añadir las claves nuevas en `app_es.arb`, justo después de
`"settingsSupportContactUs": "Contacto",` (antes de `"faqTitle"`):

```json
  "settingsSectionLegal": "Legal",
  "settingsLegalPrivacyPolicy": "Política de privacidad",
  "settingsLegalTermsOfService": "Términos de servicio",
```

En `app_en.arb`, mismo lugar:

```json
  "settingsSectionLegal": "Legal",
  "settingsLegalPrivacyPolicy": "Privacy Policy",
  "settingsLegalTermsOfService": "Terms of Service",
```

- [ ] **Step 2**: `flutter gen-l10n` desde `/Users/cris/Desktop/forge`.

- [ ] **Step 3**: En `settings_screen.dart`, añadir la nueva sección entre
"Soporte" (termina en la línea `240` actual, `const SizedBox(height: 8),`
antes de `SettingsSectionLabel(l10n.settingsSectionAbout, ...)`) y "Acerca
de":

```dart
            const SizedBox(height: 8),
            SettingsSectionLabel(l10n.settingsSectionLegal, delay: 210),
            SettingsGroup(
              delay: 220,
              children: [
                SettingsTile(
                  iconColor: const Color(0xFF64748B),
                  icon: Icons.privacy_tip_rounded,
                  title: l10n.settingsLegalPrivacyPolicy,
                  onTap: () => Navigator.push(
                    context,
                    _fadeRoute(
                      LegalDocumentScreen(
                        title: l10n.settingsLegalPrivacyPolicy,
                        documentType: LegalDocumentType.privacyPolicy,
                      ),
                    ),
                  ),
                ),
                SettingsTile(
                  iconColor: const Color(0xFF64748B),
                  icon: Icons.description_rounded,
                  title: l10n.settingsLegalTermsOfService,
                  onTap: () => Navigator.push(
                    context,
                    _fadeRoute(
                      LegalDocumentScreen(
                        title: l10n.settingsLegalTermsOfService,
                        documentType: LegalDocumentType.termsOfService,
                      ),
                    ),
                  ),
                ),
              ],
            ),
```

(Insertar esto justo antes de `SettingsSectionLabel(l10n.settingsSectionAbout,
delay: 230)` — el `delay: 230` de "Acerca de" ya deja hueco para los nuevos
`210`/`220` sin solaparse en la animación escalonada.)

- [ ] **Step 4**: Añadir el import al principio del archivo:

```dart
import '../../../legal/domain/entities/legal_document_entity.dart';
import '../../../legal/presentation/screens/legal_document_screen.dart';
```

(Ajustar la ruta relativa exacta según la profundidad real del archivo
respecto a `lib/features/` — misma que usa el import de `FaqScreen` ya
presente en este mismo archivo.)

- [ ] **Step 5**: `flutter analyze
lib/features/profile/presentation/screens/settings_screen.dart`. Sin
errores.

---

### Task 18: Verificación final Flutter

**Files:** ninguno — solo verificación.

- [ ] **Step 1**: `flutter analyze` completo desde `/Users/cris/Desktop/forge`.
Expected: sin errores nuevos respecto al estado antes de este plan (23
issues preexistentes ajenos ya documentados en el plan de FAQ no cuentan).

- [ ] **Step 2**: Recorrido manual (`flutter run`, dispositivo/emulador):
1. Ajustes → nueva sección "Legal" visible entre "Soporte" y "Acerca de",
   con dos filas.
2. Tocar "Política de privacidad" → si ya se publicó contenido desde el CMS
   (Task 9), se ve con el mismo formato (títulos, párrafos, listas,
   negritas); si no, "Este documento aún no ha sido publicado."
3. Tocar "Términos de servicio" → mismo comportamiento con el otro
   documento.
4. Sin red: estado de error con el mensaje esperado.

No se crea ningún commit en `forge`.
